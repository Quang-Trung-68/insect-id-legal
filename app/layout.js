import './globals.css';

export const metadata = {
  title: 'Insect ID Lens — Legal',
  description: 'Privacy Policy, Terms of Use, and Account Deletion for Insect ID Lens.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
